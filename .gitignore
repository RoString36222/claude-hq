# Runtime state / private data (never commit)
config.json
sessions-meta.json
claude-hq.log
claude-hq.err.log

# Build/verify artifacts
verify_*.png
verify.png
__pycache__/
*.pyc

# OS
.DS_Store

# Arena device token — never commit
arena-link.json

# Backend local/production env + venv
backend/.env
backend/.env.production
backend/.venv/
backend/*.db
backend/*.db-wal
backend/*.db-shm
backend/*.db-journal
